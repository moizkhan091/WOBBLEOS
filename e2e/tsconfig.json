{
  "//": "Typecheck scope for the E2E framework (Playwright config + e2e/**). Extends the app tsconfig so `@/…` resolves to ../src and the seed can import the real domain/services. Run: npx tsc --noEmit -p e2e/tsconfig.json",
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "incremental": false,
    "types": ["node"]
  },
  "include": ["**/*.ts", "../playwright.config.ts"],
  "exclude": ["node_modules", ".artifacts", ".auth"]
}
